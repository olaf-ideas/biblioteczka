#include <iostream>

int main() {
    std::cin.tie(nullptr);
    std::ios_base::sync_with_stdio(false);

    int a, b;

    std::cin >> a >> b;
    std::cout << a + b << "\n";
    for(int i = 1; i <= 100000000; i++);
    return 0;
}
